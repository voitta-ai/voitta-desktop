# Menu bar UI (macOS only)
rumps>=0.4.0

# Terminal UI
textual>=0.70.0

# LLM reverse proxy
aiohttp>=3.9.0

# OAuth2
msal>=1.28.0
requests>=2.31.0

# MCP proxy
fastmcp>=2.10.0,<4   # 4.x moved to httpx2 / mcp 2.x; code targets fastmcp 3
httpx>=0.27          # imported directly by mcpproxy/server.py

# macOS native UI
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-WebKit>=10.0

# Config
python-dotenv>=1.0.0

# Image processing (thumbnails)
Pillow>=10.0

# Brotli response decoding (Anthropic serves br-encoded bodies)
brotli>=1.1.0
