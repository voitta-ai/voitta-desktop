<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Python interpreter dlopens C extensions; without these, hardened runtime
         blocks them and the bundled app won't launch. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- LLM proxy (outbound to api.anthropic.com) + MCP backends. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Local listeners on 18900/18765 for Claude Code to reach. -->
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
