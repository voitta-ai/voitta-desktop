"""Voitta Desktop briefcase entry-point package.

Empty by design — the real logic lives in app.py at the repo root and in the
sibling root-level packages (auth, mcpproxy, middleware, optimizers, proxy,
ui, config). This package exists only so briefcase has an `app_name` slug
to hang the bundle off.
"""
